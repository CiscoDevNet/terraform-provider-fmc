---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "fmc_interface_groups Resource - terraform-provider-fmc"
subcategory: "Objects"
description: |-
  This resource manages Interface Groups through bulk operations.
  The following restrictions apply:
  Bulk object deletion is not supported by FMC, it will be handled one-by-oneUpdates are always done one-by-one.
---

# fmc_interface_groups (Resource)

This resource manages Interface Groups through bulk operations.

The following restrictions apply:
  - Bulk object deletion is not supported by FMC, it will be handled one-by-one
  - Updates are always done one-by-one.

## Example Usage

```terraform
resource "fmc_interface_groups" "example" {
  items = {
    my_interface_groups = {
      interface_type = "ROUTED"
      interfaces = [
        {
          id = "0050568A-4E02-1ed3-0000-004294969198"
        }
      ]
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `items` (Attributes Map) Map of Interface Groups. The key of the map is the name of the individual Interface Group. (see [below for nested schema](#nestedatt--items))

### Optional

- `domain` (String) Name of the FMC domain

### Read-Only

- `id` (String) Id of the object

<a id="nestedatt--items"></a>
### Nested Schema for `items`

Required:

- `interface_type` (String) All interfaces' types must match the interface mode.
  - Choices: `PASSIVE`, `INLINE`, `SWITCHED`, `ROUTED`, `MANAGEMENT`, `LOOPBACK`

Optional:

- `interfaces` (Attributes Set) (see [below for nested schema](#nestedatt--items--interfaces))

Read-Only:

- `id` (String) Id of the Security Zone.
- `type` (String) Type of the object; this value is always 'InterfaceGroup'.

<a id="nestedatt--items--interfaces"></a>
### Nested Schema for `items.interfaces`

Optional:

- `id` (String) Id of the interface.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# <domain> is optional. If not provided, `Global` is used implicitly and resource's `domain` attribute is not set.
terraform import fmc_interface_groups.example "<domain>,[<item1_name>,<item2_name>,...]"
```
